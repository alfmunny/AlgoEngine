# AlgoEngine

Conan
CMake

## install libraries

```sh
mkdir build 
cd build
conan install .. --build=missing
ln -s compile_commands.json ../ # for vim plugin to find the header files
```

## Configure

```sh
./configure.sh
```

## Build
```sh
./build.sh

```

## Run

```sh
./run.sh
```
